#pragma code_page(65001)

// aw-server.exe 的内嵌图标。
//
// 任务管理器 / 资源管理器只读 exe 里的图标资源，所以缺了它进程就显示系统通用图标。
// app.ico 由上层 aw-qtui 仓库的 `just icon` 生成，`just server` 会把它拷进本目录；
// 文件缺失时 build.rs 直接跳过，服务端功能不受影响。

#define IDI_APP_ICON 101

IDI_APP_ICON ICON "app.ico"
